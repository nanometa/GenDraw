// Re-export of the deployed GenDraw contract address (Requirement 12.4).
// Re-exported here so consumers can `import { CONTRACT_ADDRESS } from
// '@gendraw/contract/address'` without pulling in the rest of the
// network configuration.
export { CONTRACT_ADDRESS } from './config.js';
